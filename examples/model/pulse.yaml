pulse:
  behaviors:
    - id: process-input
      name: Process input
  pulses:
    - id: input-submitted
      display: "01"
      name: Input submitted
  flows:
    - trigger: User submits a sufficiently long input to demonstrate deterministic trigger text wrapping
      pulse: input-submitted
      to: process-input
